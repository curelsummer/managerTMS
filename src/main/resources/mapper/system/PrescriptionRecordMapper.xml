<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.system.dao.PrescriptionRecordMapper">

    <!-- 根据治疗记录ID查询标准处方记录 -->
    <select id="selectByTreatmentRecordId" resultType="cc.mrbird.febs.system.domain.vo.PrescriptionRecordVO">
        SELECT 
            id,
            treatment_record_id,
            patient_pres_id,
            pres_strength,
            pres_freq,
            last_time,
            pause_time,
            repeat_count,
            total_count,
            total_time_str,
            pres_part,
            standard_pres_name,
            periods,
            pres_date,
            pres_time,
            create_time,
            update_time
        FROM prescription_record 
        WHERE treatment_record_id = #{treatmentRecordId}
        ORDER BY id ASC
    </select>

</mapper>
