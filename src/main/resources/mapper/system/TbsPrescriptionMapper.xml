<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.system.dao.TbsPrescriptionMapper">

    <!-- 根据治疗记录ID查询TBS处方记录 -->
    <select id="selectByTreatmentRecordId" resultType="cc.mrbird.febs.system.domain.vo.TbsPrescriptionVO">
        SELECT 
            id,
            treatment_record_id,
            patient_pres_tbs_id,
            pres_strength,
            inner_freq,
            inner_count,
            inter_freq,
            inter_count,
            pause_time,
            repeat_count,
            total_count,
            total_time_str,
            pres_part,
            periods,
            tbs_type,
            pres_date,
            pres_time,
            create_time,
            update_time
        FROM tbs_prescription 
        WHERE treatment_record_id = #{treatmentRecordId}
        ORDER BY id ASC
    </select>

</mapper>
