<configuration>
    <property name="MQTT_LOG_DIR" value="logs/mqtt"/>

    <!-- 控制台输出，便于看到启动期异常堆栈 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 应用通用文件输出，便于在看不到控制台时排查 -->
    <property name="APP_LOG_DIR" value="logs/app"/>
    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${APP_LOG_DIR}/%d{yyyy-MM-dd}/app.log</fileNamePattern>
            <maxHistory>14</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="MQTT_SIFT" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator class="ch.qos.logback.classic.sift.MDCBasedDiscriminator">
            <key>direction</key>
            <defaultValue>inbound</defaultValue>
        </discriminator>
        <sift>
            <appender name="MQTT_${direction}" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                    <fileNamePattern>${MQTT_LOG_DIR}/%d{yyyy-MM-dd}/${direction}.log</fileNamePattern>
                    <maxHistory>30</maxHistory>
                    <totalSizeCap>2GB</totalSizeCap>
                </rollingPolicy>
                <encoder>
                    <pattern>%msg%n</pattern>
                </encoder>
            </appender>
        </sift>
    </appender>

    <appender name="ASYNC_MQTT" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="MQTT_SIFT"/>
    </appender>

    <logger name="MQTT_AUDIT" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_MQTT"/>
    </logger>

    <!-- Root 日志器，保证常规日志/异常都会输出到控制台 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APP_FILE"/>
    </root>
</configuration>